/**
 * This is a sample file.
 * Rename it as config.js and modify the values.
 *   database name
 *   username
 *   password
 *   host
 *   dialect Any dialect understood by sequelize
 */
const config = {
  database: 'database',
  username: 'username',
  password: 'password',
  host: 'localhost',
  dialect: 'mysql',
};

module.exports = config;
